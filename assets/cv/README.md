Drop your CV here as `nick-williams-cv.pdf`.

The "Download CV" button on the homepage already points at
`assets/cv/nick-williams-cv.pdf` — once the file exists at that path, the
button works with no other changes.
